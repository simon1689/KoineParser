import { GreekStylerPipe } from './greek-styler.pipe';

describe('GreekStylerPipe', () => {
  it('create an instance', () => {
    const pipe = new GreekStylerPipe();
    expect(pipe).toBeTruthy();
  });
});
